syntax = "proto2";

package crate.self.async;

message Msg {
    optional bool super = 1;
    optional bool i32 = 2;
    optional bool type = 3;

    oneof try {
        int32 as = 4;
    }
}

enum Enum {
    ENUM_1 = 1;
    ENUM_SELF = 2;
}
