[package]
name = "encode-only"
version = "0.0.0"
edition = "2021"

[dependencies]
micropb = { path = "../../micropb/", default-features = false, features = ["encode", "container-heapless"]}

[build-dependencies]
micropb-gen = { path = "../../micropb-gen/" }

